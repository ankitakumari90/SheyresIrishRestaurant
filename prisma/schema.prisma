generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  
 
}

model Reservation {
  id     String   @id @default(uuid())
  name   String
  email  String
  tel    String
  guests Int
  date   DateTime
  time   String
}

